import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-seed-fund-launch',
  standalone: true,
  imports: [CommonModule, RouterLink],
  templateUrl: './seed-fund-launch.component.html',
  styleUrl: './seed-fund-launch.component.scss'
})
export class SeedFundLaunchComponent {
  
}
